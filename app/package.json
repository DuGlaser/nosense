{
  "private": true,
  "version": "0.0.0",
  "name": "app",
  "main": "main/index.js",
  "license": "MIT",
  "author": "DuGlaser <duglaser0618@gmail.com>",
  "homepage": "https://github.com/DuGlaser/nosense",
  "scripts": {
    "build": "npm run clean && tsc -p .",
    "dev": "npm run build && electron main",
    "lint": "run-p lint:**",
    "lint-fix": "run-s lint-fix:**",
    "lint:eslint": "TIMING=1 eslint .",
    "lint-fix:eslint": "npm run lint:eslint --fix",
    "clean": "run-p clean:**",
    "clean:main": "rm -rf main/",
    "clean:dist": "rm -rf dist/",
    "release": "npm run clean:dist && electron-builder --dir --config electron-builder.json"
  },
  "dependencies": {
    "electron-is-dev": "2.0.0",
    "electron-next": "3.1.5"
  },
  "devDependencies": {
    "electron": "20.0.3",
    "electron-builder": "23.3.3",
    "eslint-plugin-custom": "*"
  }
}
