{
  "name": "@nosense/damega",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "scripts": {
    "build": "run-s clean build:typescript resolve-path",
    "build:typescript": "tsc -d",
    "clean": "rm -rf dist",
    "resolve-path": "ts-node ./scripts/resolve-path.ts",
    "test": "jest",
    "test:dev": "jest --watch",
    "lint": "run-p lint:**",
    "lint-fix": "run-s lint-fix:**",
    "lint:eslint": "TIMING=1 eslint './**/*.{ts,tsx}'",
    "lint-fix:eslint": "npm run lint:eslint --fix"
  },
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "eslint-plugin-custom": "*",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "ts-node": "^10.8.1"
  }
}
