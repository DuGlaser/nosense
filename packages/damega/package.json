{
  "name": "@nosense/damega",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:dev": "jest --watch",
    "lint": "run-p lint:**",
    "lint-fix": "run-s lint-fix:**",
    "lint:eslint": "TIMING=1 eslint './**/*.{ts,tsx}'",
    "lint-fix:eslint": "npm run lint:eslint --fix"
  },
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "eslint-plugin-custom": "*"
  }
}
